<div class="container"> 
  <div class="page-header">
    <h1><%= parent.title %></h1>
  </div>
  <div class="row">
    <article class="col-xs-12">
      <%= render_slideshow_for(parent) %>
      <%= render_copy_text(parent.body) %>
    </article>
  </div>
  <div id="thumbnails-partner" class="row">
    <%= render collection %>
  </div>
</div>